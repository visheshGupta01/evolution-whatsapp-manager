import { Server, Settings2, ShieldCheck } from 'lucide-react';
import { useSessions } from '../hooks/use-sessions';
export function SettingsPage(){const{data=[]}=useSessions();const api=String(import.meta.env.VITE_API_URL||'http://localhost:3000/api');return <><section className="hero"><div><p className="eyebrow">SYSTEM</p><h1>Settings</h1><p>Connection, security and workspace information.</p></div></section><div className="grid"><article className="card settingsCard"><h3><Server size={17}/>Evolution API</h3><div className="meta"><div><small>Endpoint</small><span>{api}</span></div><div><small>Sessions</small><span>{data.length}</span></div><div><small>Refresh</small><span>8 seconds</span></div></div></article><article className="card settingsCard"><h3><ShieldCheck size={17}/>Security</h3><p>Your global Evolution API key remains on the Express backend. It is never exposed to browser code.</p><div className="securityNote"><Settings2 size={15}/>BFF architecture enabled</div></article></div></>}
